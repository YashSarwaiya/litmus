# Verification Report — calculator-subtract

_Generated by `/verify-all`. Latest run: after the fix._

## Requirement
"subtract two numbers and return the result" (first minus second, order matters).

## Result
13 pass, 0 fail (Node `node:test`). The code satisfies the requirement.

| behavior | requirement | result |
|---|---|---|
| B1 | subtract two numbers → difference | PASS |
| B2 | order matters (first − second) | PASS |
| B3 | supports decimals | PASS |
| B4 | supports negatives | PASS |
| B5 | returns a number | PASS |

## History
- First run (builder used `+`): 2 pass / 11 fail — all behaviors FAIL.
- After fix (`a - b`): 13 pass / 0 fail — all behaviors PASS.

## Confidence
High — expectations match the spec verbatim (not back-fitted to the code); the
`0.3 - 0.1` case uses float tolerance; clean exit, no skips/errors.
