# Verification Dashboard — expect

_Generated by /verify-all on 2026-06-06 · mode: smart · 3 features · re-run after fix_

## Verdict: NEEDS ATTENTION — 2 PASS · 0 FAIL · 0 ERROR · 1 SKIP  (3 features)

| Feature | Status | Pass | Fail | Manual | Report |
|---------|--------|------|------|--------|--------|
| calculator-multiply | 📝 SKIP | – | – | – | (draft — run /req to lock) |
| calculator-add-red | ✅ PASS | 22 | 0 | 1 | [report](calculator-add-red/report.md) |
| calculator-subtract | ✅ PASS | 13 | 0 | 0 | [report](calculator-subtract/report.md) |

**Bottom line:** both built features pass now (`subtract` was fixed from `a + b`
to `a - b`). The only thing left is `calculator-multiply`, which is still a draft
and was not checked — so the overall verdict stays NEEDS ATTENTION (it never
reports "all clear" while anything is skipped). One manual check is pending on
`calculator-add-red` (confirm the result visibly renders red). Nothing was changed.

### Action — calculator-multiply
Still a draft. Run `/req` to lock it, then re-run `/verify-all`.
